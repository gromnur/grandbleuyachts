---
---

<section class="cta-banner">
  <div class="cta-banner-inner">
    <div class="cta-banner-text">
      <span class="cta-banner-label">Ready to Get Started?</span>
      <p class="cta-banner-sub">Whether you need management, delivery, or charter — tell me about your yacht.</p>
    </div>
    <a href="/contact" class="cta-banner-btn">
      <span class="cta-btn-top">Get in Touch</span>
      <span class="cta-btn-sub">Request a Private Consultation</span>
    </a>
  </div>
</section>

<style>
  .cta-banner {
    background: rgba(0, 26, 58, 0.72);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-top: 1px solid rgba(116, 185, 223, 0.14);
    padding: var(--spacing-xl) var(--spacing-xl);
  }

  .cta-banner-inner {
    max-width: 1040px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-xl);
  }

  .cta-banner-text {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .cta-banner-label {
    font-size: 0.68rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--blue-accent);
  }

  .cta-banner-sub {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.72);
    margin: 0;
    line-height: 1.5;
  }

  .cta-banner-btn {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    text-decoration: none;
    border: 2px solid rgba(255, 255, 255, 0.25);
    border-radius: 20px;
    padding: 10px 22px;
    flex-shrink: 0;
    transition: opacity var(--transition-fast), border-color var(--transition-fast);
    white-space: nowrap;
  }

  .cta-banner-btn:hover {
    opacity: 0.8;
    border-color: rgba(255, 255, 255, 0.45);
  }

  .cta-btn-top {
    font-size: 0.88rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255, 255, 255, 0.9);
  }

  .cta-btn-sub {
    font-size: 0.6rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: rgba(255, 255, 255, 0.55);
  }

  @media (max-width: 768px) {
    .cta-banner {
      padding: var(--spacing-lg) var(--spacing-md);
    }

    .cta-banner-inner {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: var(--spacing-md);
    }
  }
</style>
